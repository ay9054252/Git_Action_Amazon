name: Playwright Automation CI/CD

on:
  # push:
  #   branches:
  #     - main
  #     - develop

  # pull_request:
  #   branches:
  #     - main
  #     - develop

  workflow_dispatch:

  # schedule:
  #   # Every Wednesday at 1:20 PM IST (07:50 UTC)
  #   - cron: '50 7 * * 3'

env:
  NODE_VERSION: '20'
  ENVIRONMENT: 'QA'
  BASE_URL: 'https://your-application-url.com'

jobs:
  playwright-test:
    name: Run Playwright Tests
    runs-on: ubuntu-latest

    environment:
      name: QA

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install Dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      # - name: Run Smoke Tests
      #   run: npx playwright test --grep "@smoke"

      # - name: Run Regression Tests
      #   run: npx playwright test --grep "@regression"

      - name: Upload Playwright Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-html-report
          path: playwright-report/
          retention-days: 30

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results/
          retention-days: 30

      # - name: Send Email Notification
      #   if: always()
      #   uses: dawidd6/action-send-mail@v3
      #   with:
      #     server_address: smtp.gmail.com
      #     server_port: 465
      #     secure: true
      #     username: ${{ secrets.EMAIL_USERNAME }}
      #     password: ${{ secrets.EMAIL_PASSWORD }}
      #     subject: Playwright Automation Execution Report
      #     to: yourmail@gmail.com
      #     from: GitHub Actions
      #     body: |
      #       Playwright Test Execution Completed.

      #       Environment: ${{ env.ENVIRONMENT }}
      #       Repository: ${{ github.repository }}
      #       Branch: ${{ github.ref_name }}
      #       Triggered By: ${{ github.actor }}

      #       Status: ${{ job.status }}

      #       Please download the attached report from GitHub Artifacts.
      #     attachments: |
      #       playwright-report/index.html